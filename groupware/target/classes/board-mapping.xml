<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<insert id="insertBoard" parameterType="board">
		insert into 
		board(board_no, board_writer_no, board_title, board_contents, board_date, board_viewcount, board_type) 
		values ((select nvl(max(board_no),0)+1 from board),101,#{board_title},#{board_contents},sysdate,0,#{board_type})
	</insert>
	
	<!-- <select id="selectBoard" resultType="board">
		select board_no, employee_name as board_writer, board_title, board_date, board_viewcount  
		from board, employee 
		where BOARD.BOARD_NO=EMPLOYEE.EMPLOYEE_NO
	</select> -->
	
	<select id="selectBoard" resultType="board">
		select board_no, employee_name as board_writer, board_title, board_date, board_viewcount 
		from board, employee
		order by board_no desc
	</select>
	
	
</mapper>